# 4进程控制

## 1进程退出

**1.进程正常退出，结果正确，return 0**

**2.代码没有跑完，结果不正确，return !0**

**代码没有跑完，直接崩了，退出码没有意义的。**



**进程如何退出呢？**

**1.main函数return**

**2.其它函数exit和_exit函数。**

**注意：exit是用户层函数，底层封装了_eixt。 exit会刷新用户缓冲区的数据，_exit在内核，不关心用户层的数据。**

**代码示例**

```c++
#include <iostream>
#include <unistd.h>
#include <cstdio>
using namespace std;

#define opt 3

int main()
{
#if opt == 1

  cout<< "a process" <<endl;
  exit(1);

#elif opt == 2

  printf("aaaaaaaaaaaaaa");
  sleep(3);
  exit(2);
  
#elif opt == 3

  printf("aaaaaaaaaaaaaa");
  sleep(3);
  _exit(1);

#endif 
  return 0;
}
```

**总结：**
**echo $? 记录上一次进程的退出码。**

**1.main退出，2.其它函数进程exit和_exit.  3.exit和_exit 用层面和内核层面的接口的。**



## 2进程等待

**进程等待的必要性**

**1.回收子进程的资源，PCB数据结构还在内核内存里面的。子进程的退出信息的。**

**2.子进程的退出信息两大类，退出信号，退出码。**



### wait

```c
pid_t wait(int *status);
// 通过status拿到进程的退出信息。
// 输出型参数，位图结构
```

**代码示例**

```c
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

#define opt 2

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    exit(2);
  }
  
  if(id == 0)
  {
#if op1 == 1

    cout<< "子进程" << endl;
    exit(21);

#else 
    while(true)
    {
      cout<< "子进程pid:" << getpid() << endl;
      sleep(1);
    }
    exit(0);
#endif
  }

  int status = 0;
  int ret = wait(&status);
  if(ret > 0)
  {
    cout<< "子进程回收成功pid:" << ret << endl;
  }

  cout<< "exit_code:" << ((status>>8)&0xFF) << endl;
  cout<< "sign_number:" << ((status)&0x7F) << endl;

  return 0;
}

```

**总结：**

**1.wait默认是阻塞等待的**

**2.wait等待的结果是位图，高8位退出码，第七位退出信号。**



### waitpid

**wait的高级功能实现**

```c
pid_t waitpid(pid_t pid, int *status, int options);
// pid > 0 子进程的id
// pid = -1, 任意一个子进程的id
// options=0阻塞等待
// options=wnohang 实现轮询
```

**代码示例**

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

#define opt 2

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    exit(2);
  }
  
  if(id == 0)
  {
#if op1 == 1

    cout<< "子进程" << endl;
    exit(21);

#else 
    while(true)
    {
      cout<< "子进程pid:" << getpid() << endl;
      sleep(1);
    }
    exit(0);
#endif
  }

  int status = 0;
  while(true)
  {
    int ret = waitpid(id, &status, WNOHANG);
    if(ret == 0)
    {
      cout<< "本次循环没有等到子进程" << endl;
      sleep(1);
    }
    else if(ret > 0)
    {
      cout<< "子进程回收成pid:" << ret << endl;
      cout<< "exit_code:" << ((status>>8)&0xFF) << endl;
      cout<< "sign_number:" << ((status)&0x7F) << endl;
      return 12;
    }
    else 
    {
      cout<< " 运行出错了" << endl;
      return 1;
    }
  }
  return 0;
}

```

**总结：**

**WNOHANG:只会检查一下，如果没有检测立即返回的。 -1参数等待任意子的进程。**



### 四个宏

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

#define opt 2

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    exit(2);
  }
  
  if(id == 0)
  {
#if op1 == 1

    cout<< "子进程" << endl;
    exit(21);

#else 
    while(true)
    {
      cout<< "子进程pid:" << getpid() << endl;
      sleep(1);
      exit(22);
    }
    exit(0);
#endif
  }

  int status = 0;
  while(true)
  {
    int ret = waitpid(id, &status, WNOHANG);
    if(ret == 0)
    {
      cout<< "本次循环没有等到子进程" << endl;
      sleep(1);
    }
    else if(ret > 0)
    {
      cout<< "子进程回收成pid:" << ret << endl;
      
      if(WIFEXITED(status))
      {
        cout<< "exit_code:" << WEXITSTATUS(status) << endl;
      }
      else if(WIFSIGNALED(status))
      {
        cout<< "sig_num:" << WTERMSIG(status) << endl;
      }

      return 12;
    }
    else 
    {
      cout<< " 运行出错了" << endl;
      return 1;
    }
  }
  return 0;
}

```



## 3程序替换

**1.创建子进程的目的，让子进程成为父进程的一部分。执行父进程对应磁盘代码的一部分。**

**2.创建子进程的目的，让子进程成为一个全新的程序，让子进程想办法加载磁盘上的执行程序，执行新的代码和程序。**

**程序替换的本质：将指定程序的代码和数据加载到指定的位置，（指定的位置：覆盖自己的代码和数据）**

```c
#include <unistd.h>

int execl(const char *path, const char *arg, ...);
int execv(const char *path, char *const argv[]);
int execlp(const char *file, const char *arg, ...);
int execvp(const char *file, char *const argv[]);
int execle(const char *path, const char *arg, ..., char * const envp[]);
int execve(const char *filename, char *const argv[], char *const envp[]);
```

**l:list，执行代码必须直接list出来，在命令和是如何执行的，指令不能包含太多的选项的。**

**v:vector:执行代码的必须放在数组里面，数组里面放的是如何执行，同样指令不能包含太多的选项的。**

**p:path:自己到环境变量去找的。**

**e:env:自己维护的环境变量。**



### execl

**execl代码示例**

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  if(id == 0)
  {
    execl("/usr/bin/ls", "ls", "-a", "-l", "-h", "/", NULL);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结：execl:**

**没有带p，需要自己指定执行代码的位置。带了l,需要自己list在函数接口。注意结尾 必须 NULL结尾的。**



### execlp

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  if(id == 0)
  {
    execlp("ls", "ls", "-a", "-l", "-h", "/", NULL);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结：**

**execlp: l任然是自己手动list在函数接口，带了p自己去环境变量找，自己少写点。**



### execle

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  const char* const envp[] = {"NAME=lic", "AGE=20", NULL};
  if(id == 0)
  {
    execle("/usr/bin/env", "env", NULL, envp);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结：**

**execle:  同样的需要自己list出来执行的方法，需要自己指定路径，同时还需自己维护新的环境变量的**

**可以put(env): 添加新的环境变量。上面的代码是自己覆盖的。**



### execv

**代码示例**

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  char* argv[] = {"ls", "-a", "-l", "-h", NULL};
  if(id == 0)
  {
    execv("/usr/bin/ls", argv);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结：**

**execv:  1.自己指定路径，2.v自己在数组list出来的。**



### execvp

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  char* argv[] = {"ls", "-a", "-l", "-h", NULL};
  if(id == 0)
  {
    execvp("ls", argv);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结execvp:**

**1.v:需要自定义数组的。p自己的环境变量去找的的。**



### execve

```c++
#include <unistd.h>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{

  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  char* argv[] = {"ls", "-a", "-l", "-h", NULL};
  if(id == 0)
  {
    execve("/usr/bin/ls", argv, NULL);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}

```



## 4shell原理

**1.shell的一般原理是：执行的代码是一个进程，shell会为当前的进程分配一个子进程执行代码的。**

**2.有些就不是进程执行的，就是shell自己本身执行的，就是内建指令的。**

**简易版shell示例**

**代码：**

```c++
#include <unistd.h>
#include <cstring>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{
// 1.获取命令行的参数
  char buf[128] = {0};
  cout<< "输入指令:";
  char* s = fgets(buf, sizeof(buf), stdin);
  if(s == NULL)
  {
    perror("fgets");
  }
  
  /*
   *abcde\n 6-1=5;   
   */
// 2.清除\n
  buf[strlen(buf)-1] = '\0';

// 3.获取命令行的参数
   char* argv[64] = {0};
  
   argv[0] = strtok(buf, " ");
  
   int i = 1;
   while((argv[i] = strtok(NULL, " ")) != NULL)
   {
     i++;
   }

// 4.创建子进程
  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  if(id == 0)
  {
    execvp(argv[0], argv);
    perror("execvp");
    exit(1);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }

  return 0;
}
```

**总结：**

**1.一般的指令是当前shell的一个子进程，子进程崩了和shell无关的。我想看信息就echo $?**

**2.shell有时候需要自己执行的，就是自己的指令的。就是内建指令的。**



## 5高级shell原理

```c++
#include <unistd.h>
#include <cstring>
#include <iostream>
#include <sys/wait.h>
using namespace std;

int main()
{
  
  while(true)
  {
// 1.获取命令行的参数
  char buf[128] = {0};
  cout<< "输入指令:";
  char* s = fgets(buf, sizeof(buf), stdin);
  if(s == NULL)
  {
    perror("fgets");
  }
  
  /*
   *abcde\n 6-1=5;   
   */
// 2.清除\n
  buf[strlen(buf)-1] = '\0';

// 3.获取命令行的参数
   char* argv[64] = {0};
  
   argv[0] = strtok(buf, " ");
  
   int i = 1;
   while((argv[i] = strtok(NULL, " ")) != NULL)
   {
     i++;
   }


// 4.内建指令处理
  
  // 4.1cd指令
  if(argv[0] != NULL && strcmp(argv[0], "cd") == 0)
  {
    if(argv[1] != NULL)
    {
      chdir(argv[1]);
    }
    continue;
  }
  //4...后面补充需要处理的内建指令信息的。

// 5.创建子进程
  pid_t id = fork();
  if(id == -1)
  {
    perror("fork");
    return -1;
  }

  if(id == 0)
  {
    execvp(argv[0], argv);
    perror("execvp");
    exit(1);
  }

  int status = 0;
  int ret = waitpid(id, &status, 0);
  if(ret == -1)
  {
    perror("waitpid");
    return -1;
  }

  cout<<endl;
  if(WIFEXITED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "exie_code:" << WEXITSTATUS(status) <<endl;
  }
  else if(WIFSIGNALED(status))
  {
    cout<< "wait success pid:" << ret << endl;
    cout<< "sig_number:" << WTERMSIG(status) << endl;
  }
  }

  return 0;
}
```







































































